# 项目更新指南

## ✅ 已完成的更新

### 1. ESLint 配置
- ✅ 安装 ESLint 依赖
- ✅ 配置 `eslint.config.mjs`
- ✅ 添加 lint 脚本到 package.json
- ✅ 创建 `.eslintignore`
- ✅ 配置 VS Code 设置

### 2. SASS 转换
- ✅ 将 SCSS 改为 SASS 缩进语法
- ✅ 更新 `_variables.sass`
- ✅ 更新 `main.sass`
- ✅ 更新所有小型组件
- ⚠️ 大型组件需要手动调整缩进

## 🔧 需要手动完成的步骤

### 1. 安装依赖

```bash
npm install
```

### 2. 转换大型组件的 SASS 语法

以下三个组件的样式已改为 `lang="sass"`,但内部仍使用 SCSS 语法。

需要手动转换（移除分号和花括号，使用缩进）：
- `components/HeroSection.vue`
- `components/CtaSection.vue`  
- `components/ServerStats.vue`

### 3. 运行 Lint 检查

```bash
npm run lint
```

### 4. 自动修复 Lint 错误

```bash
npm run lint:fix
```

### 5. 启动开发服务器

```bash
npm run dev
```

## 📝 快速 SCSS → SASS 转换

可以使用在线工具快速转换：
- https://jsonformatter.org/scss-to-sass
- 或使用 VS Code 插件进行格式化

## 🎯 ESLint 规则说明

配置的主要规则：
- TypeScript: 警告使用 `any`，未使用变量报错
- Vue: 允许单词组件名，强制自闭合标签
- 通用: 单引号，无分号，2空格缩进

运行 `npm run lint:fix` 可自动修复大部分问题。

## ⚡ 下一步

1. 安装依赖
2. 手动完成 SASS 语法转换
3. 运行 lint 检查并修复
4. 开始开发！

